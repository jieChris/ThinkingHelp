server:
  port: 8080

spring:
  application:
    name: thinking-help-system
    
  ai:
    openai:
      api-key: sk-84762730f0e949c48bf00d14ddf215a9
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      chat:
        options:
          model: qwen-plus
          temperature: 0.7
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/thinking_help?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: aw20020826z 
    type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

knife4j:
  enable: true
  openapi:
    title: Personalized Chronic Disease Diet System API
    description: API Documentation
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.thinkinghelp.system.controller

logging:
  level:
    com.thinkinghelp.system: DEBUG

jwt:
  # 生产环境请通过环境变量 JWT_SECRET 注入高强度密钥，不要使用默认值
  secret: ${JWT_SECRET:thinking-help-jwt-secret-change-in-production-2026}
  expiration: 86400000
